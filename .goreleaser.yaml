# GoReleaser configuration
# https://goreleaser.com

version: 2

before:
  hooks:
    - go mod tidy

builds:
  - main: ./cmd/mil
    binary: mil
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/daydemir/milhouse/internal/cli.Version={{.Version}}
      - -X github.com/daydemir/milhouse/internal/cli.GitCommit={{.Commit}}
      - -X github.com/daydemir/milhouse/internal/cli.BuildDate={{.Date}}

archives:
  - format: tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}

checksum:
  name_template: 'checksums.txt'

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'

brews:
  - name: milhouse
    repository:
      owner: daydemir
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    directory: Formula
    homepage: https://github.com/daydemir/milhouse
    description: Autonomous PRD-driven development CLI powered by Claude
    license: MIT
    install: |
      bin.install "mil"
    test: |
      system "#{bin}/mil", "version"
